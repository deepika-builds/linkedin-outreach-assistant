{
  "name": "linkedin-outreach-assistant",
  "type": "module",
  "version": "1.0.0",
  "description": "Chrome extension for AI-powered LinkedIn outreach",
  "private": true,
  "scripts": {
    "build":       "node build.js",
    "build:watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
